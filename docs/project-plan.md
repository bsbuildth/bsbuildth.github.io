# แผนงานหลัก BSBuildTh

อัปเดตล่าสุด: 11 กันยายน 2026
เวอร์ชัน production: `ea1235afcddcf7c1bf8a596aa38d0a3fe9cd61c3`

## สถานะปัจจุบัน

| งาน | สถานะ | ผลลัพธ์ |
| --- | --- | --- |
| แก้ lint 60 errors/1 warning | เสร็จและเผยแพร่แล้ว | lint ผ่าน 0 errors / 0 warnings |
| ฟอร์มติดต่อ | เสร็จและเผยแพร่แล้ว | บันทึก Firestore, ป้องกันกดซ้ำ และเก็บข้อมูลไว้เมื่อล้มเหลว |
| สิทธิ์ผู้ดูแล | เปิดใช้แล้ว | `songyos2528@gmail.com` มี admin claim; Firestore Rules ตรวจสิทธิ์ฝั่งเซิร์ฟเวอร์ |
| เครื่องมือใบเสนอราคา | เปิดใช้แล้ว | แก้ไขบนฟอร์ม A4, คำนวณอัตโนมัติ, ส่วนลดแบบบาท/เปอร์เซ็นต์, ยอดภาษาไทย, เลขอัตโนมัติ, VAT, โลโก้เปิด/ปิด, draft/issued/void และ REV. |
| เครื่องมือใบเสร็จ | เปิดใช้แล้ว | สร้างจากใบเสนอราคา เลขอัตโนมัติ ยอดภาษาไทย และพิมพ์ต้นฉบับ/สำเนา A4 |
| Firebase production | เชื่อมต่อแล้ว | บันทึกร่าง อ่านรายการ ออกเอกสาร และ Rules ใช้งานบน production |
| GitHub CI/CD | เปิดใช้แล้ว | lint, unit tests, Rules tests และ build ต้องผ่านก่อน GitHub Pages deploy |
| PDF/งานพิมพ์ | ใช้งานได้ | พิมพ์ผ่านเบราว์เซอร์; ตรวจ A4 หน้าเดียว ใบเสร็จ 2 หน้า และใบเสนอราคา 70 รายการ 5 หน้าแล้ว |
| เว็บไซต์ 3D | วางแผน | ยังไม่เริ่มพัฒนา ตามคำสั่งให้ปิดงานระบบเอกสารก่อน |

## สิ่งที่ตรวจรับแล้ว

- ใบเสนอราคาตัวอย่างคำนวณ 55,010.00 + 44,500.00 = 99,510.00 บาท
- ส่วนลดท้ายบิลหักก่อนคำนวณ VAT และยอดสุทธิ โดยไม่มีคอลัมน์ส่วนลดในรายการ
- งวดชำระรวม 100% และงวดสุดท้ายรับเศษสตางค์
- รายการฟรีและรายการที่ลูกค้าจัดหาเองไม่เพิ่มยอดเรียกเก็บ
- ใบเสนอราคาเก็บ revision ที่ออกแล้วและป้องกันเลขเอกสารซ้ำ
- ใบเสร็จที่ออกแล้วแก้ไขไม่ได้ และพิมพ์ต้นฉบับ/สำเนาแยกหน้า
- เลขเอกสารว่างจะสร้างเป็น `QT-YYYYMM-ลำดับ` หรือ `RC-YYYYMM-ลำดับ` ด้วย transaction และตรวจเลขที่ออกใช้แล้ว
- ข้อมูลบริษัทบันทึกเป็นค่าเริ่มต้นส่วนตัวและนำไปใช้กับเอกสารใหม่ได้
- จำนวนเงินรวมแสดงเป็นคำอ่านภาษาไทยทั้งใบเสนอราคาและใบเสร็จ
- เอกสารยาวซ้ำหัวตาราง ไม่ตัดแถวกลางข้อความ และไม่แยกบล็อกลายเซ็น
- ผู้ใช้ทั่วไปอ่านหรือเขียน quotations, revisions และ receipts ไม่ได้ตาม Firestore Rules
- Pull Request #1–#8 ผ่าน CI และรวมเข้า `main`; งานที่เปลี่ยนตัวเว็บผ่าน GitHub Pages deploy แล้ว

## งานปรับปรุงที่ยังเลือกทำเพิ่มได้

1. เพิ่ม PDF renderer เพื่อดาวน์โหลดไฟล์ตรงและควบคุมเลขหน้าหลายหน้าเหมือนกันทุกอุปกรณ์ โดยต้องรักษาฟอนต์ไทยและคุณภาพตัวอักษร
2. ทดสอบเอกสารยาวกับเครื่องพิมพ์จริงอย่างน้อยหนึ่งรุ่น หลังจากตรวจ PDF 70 รายการผ่านแล้ว
3. ยืนยันว่าจะใช้ Google Apps Script/LINE/อีเมลแจ้งเตือนต่อหรือยกเลิก
4. พิจารณาฝังฟอนต์เอกสารที่มีสิทธิ์ใช้งาน หากต้องการให้รูปแบบตัวอักษรเหมือนกันทุกเครื่อง

งานในรายการนี้ไม่ขวางการสร้าง แก้ไข บันทึก ออกเอกสาร และพิมพ์ใบเสนอราคา/ใบเสร็จในระบบปัจจุบัน
